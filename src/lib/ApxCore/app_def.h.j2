// Auto-generated file

#pragma once

// node identity

#define VERSION     "{{ data.version }}"

#define BRANCH      "{{ data.branch }}"
#define GIT_HASH    "{{ data.hash }}"
#define GIT_TIME    "{{ data.time }}"
#define GIT_YEAR    "{{ data.year }}"

#define RELATIVE_DATA_PATH      "{{ data.data_path }}"
#define RELATIVE_LIBS_PATH      "{{ data.libs_path }}"
#define RELATIVE_PLUGINS_PATH   "{{ data.plugins_path }}"

